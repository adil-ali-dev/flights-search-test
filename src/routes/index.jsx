import React, { useState } from "react";
import PublicRoutes from "./PublicRoutes";

const MainRoutes = () => {

  return (
    <React.Fragment>
        <PublicRoutes />
    </React.Fragment>
  );
};

export default MainRoutes;
